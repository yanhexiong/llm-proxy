# Cloudflare Workers 三协议转换网关实现计划

## 1. 目标与技术方案

实现一个可自行部署的轻量网关，支持 **Anthropic Messages、OpenAI Responses、Chat Completions 六个方向互转，以及三种同协议透传**。客户端只需修改 Base URL，继续使用原协议的数据结构和上游 API Key；Worker 将请求转换成上游协议，再将响应转换回客户端协议。

技术栈确定为：

- **后端：** TypeScript + Hono，部署在 Cloudflare Workers。
- **前端：** React + Vite，使用 Workers Static Assets，与后端同域部署。
- **存储：** D1，保存别名、生成的链接和管理员会话。
- **部署：** Wrangler，提供跨平台初始化向导、自动资源配置、数据库迁移和部署检查；将“无需修改源码即可完成首次部署”作为交付要求。

转换逻辑主要参考 [CPA 的 translator](https://github.com/router-for-me/CLIProxyAPI/tree/main/internal/translator)，并对照 [sub2api 的 apicompat](https://github.com/Wei-Shaw/sub2api/tree/main/backend/internal/pkg/apicompat) 中的字段映射、流式状态处理及测试场景。实现为独立的 TypeScript 模块，无需另外运行 CPA 或 sub2api。实施时记录参考版本；直接移植的代码保留相应许可和归属。

首版按已确认的范围设计：单管理员、上游 Key 调用时透传、聊天与流式响应、函数工具调用、图片输入。暂不实现账户池、计费、负载均衡、OAuth 登录上游，以及跨协议的服务端会话存储和内置搜索工具。

## 2. URL 与代理接口

### URL 格式

统一使用三个协议标识：

| 标识 | 协议 | 客户端请求端点 |
|---|---|---|
| `messages` | Anthropic Messages | `/v1/messages` |
| `responses` | OpenAI Responses | `/v1/responses` |
| `chat` | Chat Completions | `/v1/chat/completions` |

代理路径支持两种形式：

```text
直填地址：
/{凭证}/{客户端协议}/{上游协议}/u/{上游域名及基础路径}/-

使用别名：
/{凭证}/{客户端协议}/{上游协议}/a/{别名}/-
```

其中 `/-` 分隔上游地址与客户端追加的接口路径。解析时从已知的客户端端点向前匹配，避免把上游自己的路径误认为客户端端点。

例如，客户端使用 Responses，上游只支持 Chat Completions：

```text
上游 Base URL：
api.example.com/v1

生成给 OpenAI 客户端的 Base URL：
https://api.yahenix.top/{凭证}/responses/chat/u/api.example.com/v1/-/v1

客户端实际请求：
POST .../api.example.com/v1/-/v1/responses

Worker 实际访问：
POST https://api.example.com/v1/chat/completions
```

### 拼接规则

- 输入上游地址时允许省略 `https://`，也接受完整 HTTPS 地址；展示和生成路径时去掉该前缀。
- 上游 Base URL 表示接口的公共前缀，可以是 `/v1`、`/api/v1` 等自定义路径。Worker 只追加目标协议的 `messages`、`responses` 或 `chat/completions`，不擅自补第二个 `/v1`。
- 输入框提示用户填写基础地址；如果填写了完整接口地址，提示修正并展示解析结果。
- 不接受带用户名密码、查询参数或 fragment 的上游基础地址；API Key 继续放在请求头。
- OpenAI 客户端生成的 Base URL 以 `/-/v1` 结尾；Anthropic SDK 使用的地址以 `/-` 结尾。生成器同时提供完整端点，供要求填写完整 URL 的客户端使用。该区别来自 SDK 的路径追加方式：[OpenAI SDK](https://github.com/openai/openai-node/blob/master/src/client.ts)、[Anthropic SDK](https://github.com/anthropics/anthropic-sdk-typescript/blob/main/src/resources/messages/messages.ts)。
- 请求端点必须符合链接声明的客户端协议；不匹配时返回明确错误，不猜测转换方向。

### 链接凭证与别名

- 每次生成链接创建一条独立授权记录，绑定转换方向，以及一个固定上游地址或别名。
- 使用 `链接ID.HMAC签名` 作为 URL 凭证；签名密钥保存在 Workers Secrets。授权记录不可直接改换协议或目标，改变这些信息需生成新链接。
- 每次调用检查授权是否有效，支持逐条撤销；默认不自动过期。登录后可以再次复制已有链接。
- 直填地址的链接固定访问原地址。**别名链接跟随别名更新**，修改上游后已有客户端自动访问新地址。
- 修改别名时展示受影响链接数量，并提示客户端现有 Key 将发送到新上游。删除别名同时撤销关联链接，重新创建同名别名不会恢复旧授权。
- 普通上游 Base URL 无需保密；生成后的完整代理链接包含网关访问凭证，应按访问凭证管理。

## 3. 协议转换与转发行为

### 转换模块

采用三种协议适配器，共享能够表达内容块、工具调用和用量的内部类型。每个适配器负责请求解码、请求编码、响应编码和流式事件处理，保留各协议所需的信息。

重点实现：

- system、developer、instructions 和多轮消息的映射，保留消息及内容块顺序。
- 文本、图片 URL、Base64 图片之间的格式转换；Worker 不主动下载图片。
- 工具定义、工具选择、调用 ID、参数、结果以及并行工具调用的对应关系。
- 常见采样参数、输出长度限制、结束原因与用量统计；正确区分缓存读取、缓存写入与普通输入 token。
- 模型名称原样传给上游，不自动替换模型。
- 转到 Messages 时，如果客户端未提供输出长度限制，使用可配置默认值 `4096`。

同协议请求走直接透传路径，保留原始 JSON 和响应流，避免因重新构造请求而丢失扩展字段。

### 兼容策略

采用“尽量兼容，但不静默损坏请求”的原则：

- 有明确对应关系的字段进行转换；不影响生成含义的纯元数据允许忽略。
- 无法表达的关键参数、内容块或工具类型，返回包含字段位置和原因的客户端协议错误。
- 跨协议不伪造思考签名、加密推理状态、原生搜索或代码执行能力。
- 跨协议使用 `previous_response_id`、要求服务端存储的 `store:true`、后台执行等功能时明确拒绝；普通完整历史请求可以转换。
- 跨协议首版只支持单个生成结果；`n > 1` 明确拒绝。
- 为支持、降级和拒绝的字段建立兼容表，并与转换测试共同维护。

### SSE 与错误处理

使用增量 SSE 解析器和每请求独立的状态机，处理跨网络分块的 UTF-8、事件帧、工具参数及多个内容块。通过 Workers Streams 持续输出，保留取消传播和背压，避免缓存整条响应流。[Workers Streams 文档](https://developers.cloudflare.com/workers/runtime-apis/streams/)

验收必须保证：

- Responses 的增量、各类 `done` 事件和最终 response 内容一致。
- Messages 的内容块正常开始、结束，最终停止原因正确。
- Chat Completions 的 delta、finish reason、usage 和 `[DONE]` 顺序正确。
- 同一响应的 ID、工具 ID 和索引稳定；不会混入另一种协议的 SSE 事件。
- 流意外中断不伪装成成功完成；只聚合构建最终事件所需的内容，并设置可配置内存上限。

上游 HTTP 错误保留状态码，按客户端协议转换错误体，并保留 `Retry-After` 等有效信息。已经开始流式返回时，通过对应协议可表达的错误事件或连接异常结束，不补造成功事件。

上游 Key 从 `Authorization` 或 `x-api-key` 提取，再按上游协议设置请求头；转到 Messages 时补齐版本头。管理员 Cookie 和链接凭证不发送给上游。默认不自动重试生成请求、不自动跟随重定向，避免重复调用或把凭据转发到其他地址。

## 4. 管理前端、登录与存储

### 前端页面

| 页面 | 功能 |
|---|---|
| 登录 | 用户名密码登录、错误提示、退出登录 |
| 链接生成器 | 选择客户端协议、上游协议，填写地址或选择别名，生成链接 |
| 别名管理 | 新建、修改上游地址、删除、查看关联链接数量 |
| 链接管理 | 查看转换方向和目标、再次复制、撤销、重新生成 |

生成器应提供以下直接可用的结果：

- 客户端 Base URL，一键复制。
- 完整请求端点，一键复制。
- 实际上游请求地址预览。
- “客户端协议 → 上游协议 → 返回客户端协议”的说明。
- 对应 SDK 配置和 cURL 示例，API Key 使用占位符。

生成操作不访问上游、不消耗模型额度。首版不保存上游 Key，不加入聊天窗口或在线调试台。

### 基础鉴权

- 单管理员，不开放注册。
- 管理员用户名和盐化密码摘要通过部署配置及 Secrets 初始化；提供本地生成摘要的脚本。
- 使用随机会话令牌，D1 仅存摘要；Cookie 设置 `HttpOnly`、`Secure`、`SameSite`，有效期默认七天。
- 注销使会话失效；管理写接口校验同源请求，登录接口加入失败限流。
- 管理登录态仅用于后台；模型调用使用生成链接中的独立凭证。

### 管理接口与数据

管理 API 使用 `/api/admin/*`，包括登录、注销、当前会话、别名 CRUD、链接创建/查询/撤销。链接创建输入为客户端协议、上游协议及“直接地址或别名”二选一；返回 Base URL、完整端点、上游地址预览和示例配置。

D1 保存三类核心记录：别名、链接授权、管理员会话。别名使用稳定内部 ID，避免同名重建误绑定。

不保存对话正文和上游 Key。运行日志只记录请求 ID、链接 ID、协议方向、耗时、状态和错误类型，不记录完整凭证 URL 或认证头。

## 5. 部署友好要求

部署友好是首版必须满足的要求。目标是：用户取得源码、登录 Cloudflare 并设置管理员账密后，即可通过引导完成部署，无需自行拼装数据库、手工执行 SQL 或修改业务源码。

### 默认部署流程

- 保持一个 Worker、一套静态前端和一个 D1 数据库的结构，无需 VPS、Docker、外部数据库或额外部署 CPA/sub2api。默认使用 `workers.dev` 域名，自定义域名作为可选后续配置；示例中的 `api.yahenix.top` 不得硬编码。
- 项目根目录提供统一的包管理入口、锁文件和明确的 Node.js/package manager 版本要求。初始化及部署脚本使用跨平台 Node.js 实现，支持 Windows PowerShell、macOS 和 Linux，不依赖 Bash 专用命令。
- 获取源码并安装文档指定的 Node.js 与 pnpm 后，标准命令为 `pnpm install --frozen-lockfile`、`pnpm setup`、`pnpm deploy`。`setup` 引导 Cloudflare 登录及账户选择，收集 Worker 名称、管理员用户名和密码；域名等非必要项使用默认值。
- 初始化向导自动创建或复用本项目的 D1 数据库、配置绑定、生成盐化密码摘要和随机链接签名密钥，并将敏感配置写入 Workers Secrets。用户不需要手工生成密钥或复制数据库 ID；本地部署状态与敏感文件排除出版本控制。
- `deploy` 执行构建、必要配置检查、数据库迁移和发布。首次运行先确保资源可用，再按绑定名执行迁移；迁移或构建失败时停止发布。成功后输出管理页面地址，并检查页面、Worker 与数据库是否就绪；检查不调用上游模型。
- 初始化和部署可重复执行：复用既有资源与已应用迁移，不覆盖管理员凭据、不重置签名密钥、不丢失别名或链接。中途失败后可重试，错误信息指出失败步骤及修复办法。

### 便捷部署入口与文档

- 项目以公开 GitHub/GitLab 仓库交付时，在 README 提供官方 `Deploy to Cloudflare` 按钮，复用同一构建和迁移流程。按钮部署支持自动配置 D1；所需配置提供中文说明，密码摘要和签名密钥由配套初始化工具生成。仓库私有时使用命令行向导，不将公开仓库作为自用部署的前提。[Cloudflare 部署按钮文档](https://developers.cloudflare.com/workers/platform/deploy-buttons/)
- README 首屏给出最短部署步骤；另附完整配置表，列明每项用途、默认值和是否必填，并提供不含真实凭据的环境变量示例。只需部署的用户无需阅读协议转换实现。
- 提供 `pnpm doctor` 检查账户权限、数据库绑定、迁移状态及必要 Secrets 是否配置；不显示 Secret 内容。公开 `/health` 仅返回总体就绪状态，详细诊断通过命令行或管理员登录后查看。
- 文档包含自定义域名配置、管理员密码重置、常见构建/权限/数据库错误的处理，以及版本升级、数据库备份与恢复步骤；标明所用 Cloudflare 资源及可能产生费用的环节，不承诺所有用量均免费。

### 升级与回滚

- 升级沿用已有资源、域名、管理员配置和签名密钥；拉取指定版本后执行部署命令即可，已有链接保持有效。
- 数据库迁移采用兼容上一应用版本的增量方式。文档明确区分 Worker 版本回滚与数据库恢复，回滚应用不自动删除数据或逆向执行迁移；涉及不兼容变更时提供单独迁移说明。
- 每次发布提供版本号和变更说明，记录已验证的工具版本及升级路径。

## 6. 实施顺序与验收

按以下顺序交付，各阶段完成相应测试后再进入下一阶段：

1. **Worker 基础与路由：** 工程、D1 迁移、URL 解析、授权、上游转发和三种同协议透传。
2. **转换核心：** 六方向非流式转换，再完成六方向 SSE、工具调用、图片和错误处理。
3. **管理前端：** 登录、别名管理、链接生成、复制及撤销。
4. **部署与验证：** 跨平台初始化向导、自动资源配置、部署检查、升级与回滚说明、Workers 运行时测试、浏览器测试及可选真实上游验证。

自动化测试使用 Vitest 的 Workers 运行环境和模拟上游，前端使用 Playwright。至少覆盖：

- 六个转换方向 × 流式/非流式，以及三种同协议透传。
- 多轮文本、图片、完整工具调用往返、并行工具参数、缓存用量和输出截断。
- 中文被拆到不同网络块、多个事件合并、延迟到达的工具名、空结束事件、流异常中断。
- 自定义上游路径、尾斜杠和 `/v1` 拼接；通过实际 OpenAI、Anthropic SDK 验证生成的 Base URL。
- 登录失败与注销、凭证篡改与撤销、别名更新后旧链接跟随、删除别名后旧链接失效。
- 上游 401、429、5xx、超时和客户端取消，以及认证头正确转换且不泄漏管理员 Cookie。
- 在无现有项目资源的环境中，按 README 从零部署并登录，创建别名和生成链接；无需修改业务源码或手工执行 SQL。
- 初始化与部署重复执行、失败后重试、升级后旧链接可用，以及迁移失败时停止发布；在 Windows 和 Linux 上验证脚本，发布前完成 macOS 冒烟验证。
- 默认 `workers.dev` 地址和自定义域名均可生成正确链接；公开仓库交付时实际验证部署按钮流程。

最终交付包含源码、数据库迁移、初始化及部署脚本、Wrangler 配置、环境变量示例、诊断命令、兼容性说明，以及首次部署、升级、回滚和排错文档。真实上游测试使用实施时提供的 Key，凭据不进入仓库。

验收标准是：**管理员登录后，填写上游地址或建立别名，选择协议方向即可生成可用的 Base URL；客户端只修改地址并填写原上游 Key，就能完成支持范围内的聊天、流式输出、图片输入和工具调用往返。**

部署验收标准是：**用户按 README 完成账户授权和少量必要配置后即可部署并登录；重复部署与正常升级保留已有数据、凭据和链接，失败时能够按明确提示恢复操作。**
